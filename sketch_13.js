// --------------------------------------------------
// SCREEN 13
// --------------------------------------------------
function drawScreen13() {

  // BACKGROUND
  background("lightgrey");

  // --------------------------------------------------
  // TITLE
  // --------------------------------------------------
  fill("Black");
  noStroke();
  textAlign(CENTER, CENTER);
  textSize(42);

  text("Congratulations!", width / 2, 120);
text("Temple Explorer!", width / 2, 320);
}